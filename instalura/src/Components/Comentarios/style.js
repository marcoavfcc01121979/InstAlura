import { StyleSheet } from 'react-native'

const style = StyleSheet.create({
  inline: {
    flexDirection: "row",
    alignItems: "center"
  },
  imgSend: {
    width: 30,
    height: 30,
    marginRight: 5
  }
})

export default style;