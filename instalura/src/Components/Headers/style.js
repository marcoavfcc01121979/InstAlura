import { StyleSheet } from 'react-native';

const style = StyleSheet.create({
  fotoUsuario: {
    width: 50,
    height: 50,
    margin: 10,
    borderRadius: 50
  },
  headers: {
    flexDirection: "row",
    alignItems: "center",
  }
});

export default style;